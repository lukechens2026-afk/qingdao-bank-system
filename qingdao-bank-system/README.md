# qingdao-bank-system

This folder is an English-named copy of the original project. It contains the same source code and structure but uses an English project folder name and package/meta names (no Chinese in file/folder names).

To run locally:

```bash
cd qingdao-bank-system
npm install
npm run dev
```
